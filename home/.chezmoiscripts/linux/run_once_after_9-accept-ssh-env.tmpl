{{- if (and (eq .chezmoi.os "linux") .hasSudo (not .shared)) -}}

#!/usr/bin/env bash

set -euo pipefail

sudo tee /etc/ssh/sshd_config.d/accept-env.conf >/dev/null <<EOT
#: Oh My Posh config
AcceptEnv OMP_CONFIG
EOT

{{- end -}}
