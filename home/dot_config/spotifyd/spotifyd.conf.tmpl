[global]
# `spotifyd authenticate` writes the OAuth login below this private cache.
# Keep that login outside Git and use Spotify's account service instead of LAN discovery.
cache_path = {{ joinPath .chezmoi.homeDir ".cache/spotifyd" | quote }}
disable_discovery = true
device_name = {{ .chezmoi.hostname | quote }}
initial_volume = 70
